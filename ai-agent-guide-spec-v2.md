# AI Agent Guide 长期工程化 Spec 文档

版本：v2.0
基准：对 `xiaoqianbaobao/ai-guide` 仓库源码的实际盘点（非首页推测）
定位：把这个站点从"个人持续更新的博客"升级为"有版本管理、有质量门槛、能对齐 AI 前沿演进的长期知识工程"

---

## 一、现状结构化盘点

### 1.1 内容资产真实统计

| 模块 | 现有文章数 | 平均篇幅 | 首页标注状态 | 实际情况 |
|---|---|---|---|---|
| 00-preface | 3 | ~9000字 | 已成型 | 与标注一致 |
| 01-llm-foundations | 13 | ~9200字 | 已成型 | 覆盖到 chapter-06d，深度足够 |
| 02-agent-core | 3 | ~21900字 | 已成型 | 单篇篇幅全站最长之一，质量高 |
| **03-memory** | **2** | ~12100字 | 持续完善 | **篇数严重不足，是全站最薄弱模块** |
| **04-multi-agent** | **2** | ~9400字 | 持续完善 | **同上** |
| 05-tools-frameworks | 11 | ~22900字 | 持续完善 | 实际是全站投入最重的模块，Java/Spring 背景优势区 |
| 06-eval-evolution | 3 | ~11200字 | 持续完善 | 中等，需要为新趋势扩容 |

合计 37 篇正文，距目标 52 篇差 **15 篇**，且缺口高度集中在 03、04 两个模块。

### 1.2 组件资产盘点（关键发现）

站点在 `.vitepress/theme/components/` 下已经构建了 8 个自定义组件，但使用率极不均血：

| 组件 | 功能 | 全站引用次数 |
|---|---|---|
| ArticleHeader | 阅读时间/前置知识/摘要 | 已在几乎每篇正文使用 |
| KnowledgeMap | 模块知识地图定位 | 已在每篇正文使用 |
| PaginationNav | 上下篇导航 | 已使用 |
| CodeDemo / AgentFlow | 代码演示、Agent流程可视化 | 部分使用 |
| **Quiz** | 交互式选择题、正确性反馈 | **0 次** |
| **ResourceCard** | 资源卡片 | **0 次** |
| ReadingModeToggle | 专注阅读/隐藏侧边栏 | 已挂载全局 |

**结论：Quiz 和 ResourceCard 是"建了但没用"的沉没资产。** 这是本次盘点里性价比最高的优化点——不需要新写代码，只需要把它们嵌入现有 37 篇文章，就能立刻提升学习体验（见第四章）。

### 1.3 结构性遗留问题

- `docs/agent/` 和 `docs/models/python-basics/` 两个目录有实际内容（Agent概念页、编程助手实战案例、5节Python课），但首页导航完全未链接，处于游离状态。需要在本轮规划中明确：整合进主线，还是归档删除。
- `docs/01-llm-foundations/transformer-attention-qkv.md` 是旧版"总览页"，chapter-01~06d 是拆分后的新结构，两者共存，需确认旧总览页是否已完全被新导航取代，避免死链或内容重复。

---

## 二、短期指令（0-4周，内容层面）

按优先级排序，每条都是可以直接执行的任务，不是方向性建议：

1. **补齐 04-multi-agent（优先级最高，当前只有2篇，且是"系统扩展"定位模块）**
   建议新增：
   - 《多 Agent 的失败模式与恢复策略》——`orchestrator-subagent.md` 结尾已经埋了"失败一/二/三"的伏笔，直接展开成独立文章
   - 《多 Agent 的成本与延迟权衡》——token消耗随角色数量增长的工程账，什么时候多Agent是净损耗
   - 《Blackboard / Debate 等非层级协作拓扑》——目前只有 Orchestrator-Subagent 一种拓扑，缺少对比

2. **补齐 03-memory**
   建议新增：
   - 《记忆的写入时机与遗忘策略》——`four-memory-types.md` 结尾提到"怎样控制噪声和过期信息"，同样是可以直接展开的伏笔
   - 《向量库与图存储的选型对比》——从"能存"到"该存在哪"的工程决策
   - 《从 Episodic 到 Semantic 的蒸馏流程实战》——一篇偏实战的记忆压缩pipeline

3. **把 Quiz 和 ResourceCard 组件接入现有文章**
   不需要新写内容，从 02-agent-core 和 05-tools-frameworks（篇幅最长、最适合做知识检验）开始，每篇结尾加1-2道 Quiz。这是成本最低、见效最快的一步。

4. **处理游离目录**
   对 `docs/agent/` 和 `docs/models/python-basics/`做出明确决定：如果保留，需要在导航里给出入口说明（比如作为"先修课"挂在 guide 模块下）；如果不再维护，直接从仓库移除，避免搜索引擎和读者进入死角内容。

---

## 三、中长期内容规划：对齐 AI 发展趋势

这部分回应你提到的"本体论、自进化 Skills、数据治理、评测"——建议不是简单往现有7模块里插文章，而是新增两个模块 + 大幅扩容评估模块，形成完整的"下一代 Agent 工程"知识线：

### 新增模块 07：Ontology & 知识表示（本体论与知识建模）

这条线正好可以和你之前公众号写过的"西方哲学 ontology / universals debate 连接 AI 本体建模"那篇文章打通，把哲学讨论落到工程规范上：

- Agent 系统里"本体"到底解决什么问题（实体消歧、关系建模、跨系统语义对齐）
- Ontology vs Taxonomy vs Knowledge Graph 的边界
- Hypergraph 在企业级多 Agent 架构里的实际应用场景
- Schema 设计如何影响 Agent 的推理稳定性（这条可以直接呼应你已有的 03-memory 和 04-multi-agent 内容，形成模块间交叉引用）

### 新增模块 08：自进化 Skills 与 Agent 自我改进

- Skill 的自动发现与自动打包机制（区别于 05-tools-frameworks 里 Agent Skills 那篇讲的"人工设计的技能包"，这里讲"系统自己总结出技能"）
- 自我修正循环：从执行失败到生成新 Skill 的闭环设计
- 自进化机制的失控风险与护栏设计（技能污染、能力漂移、版本回滚机制）——这块建议结合你支付清结算背景，强调强监管场景下"自进化"必须要有的审计和回滚要求，这是市面上通用教程完全不会写的稀缺内容

### 新增模块 09：数据治理（Agent 时代的数据合规与质量）

- Agent 训练/记忆/RAG 语料的数据血缘（哪些数据被用来影响了哪次决策，出了问题能不能追溯）
- 敏感数据在 Memory 和 Tool 调用链路里的隔离设计
- 结合金融强监管场景：数据留存周期、审计日志、PII脱敏在 Agent pipeline 里怎么落地——这同样是你独特的领域优势

### 06-eval-evolution 扩容

现有3篇偏"怎么评估单次输出"，建议补：
- 线上持续监控与劣化检测（区别于离线benchmark）
- 评估体系本身的版本管理（评估标准也会过时，怎么迭代评估标准）

---

## 四、工程化治理机制（让内容站变成"工程"而不是"博客"）

这是本次规划里最关键的一层——没有治理机制，52篇也好、70篇也好，早晚会退化成博客式的零散更新。建议引入四个机制：

1. **时效性标注**：每篇文章 frontmatter 增加 `last-verified` 字段，注明"内容最后核实日期"。AI领域变化快，一篇讲框架选型的文章6个月不更新就可能过时，需要有机制提醒复核，而不是永久挂着旧结论。

2. **选题看板文档化**：把第二、三章列的待办篇目，整理进仓库根目录一个 `ROADMAP.md`，每篇状态标记为 `草稿/待审/已发布/待复核`。这样"52篇目标"从首页的一句话变成可追踪的任务列表。

3. **单篇质量门槛 checklist**（发布前自查）：
   - 是否有 ArticleHeader（阅读时间/前置知识）
   - 是否至少一张 mermaid 图或架构图
   - 是否至少一个可运行代码示例
   - 结尾是否有 Quiz（检验理解）和"下一步"跳转
   - 是否在 KnowledgeMap 里正确挂载

4. **模块间交叉引用机制**：新增的 07/08/09 模块和已有 03/04 模块之间要建立"回链"——比如 07-ontology 讲 Schema 设计时，应该反向链接回 03-memory 的记忆分类文章，避免新模块变成孤岛。

---

## 五、前端设计优化：让用户更专注、成长更快

你的站点已经比大多数个人知识站做得更完整——ReadingModeToggle（专注模式/隐藏侧边栏）、KnowledgeMap（知识地图定位）、ArticleHeader（前置知识提示）都已经是很扎实的设计。真正的问题不是"缺功能"，而是**已建的功能没有被内容和交互充分激活**。建议按这个优先级来：

### 优先级1：激活已有组件，而不是新建功能

- Quiz 组件已经写好交互逻辑（选择、反馈、解析），0引用是最大的浪费。建议每篇文章结尾强制加入1-2道题，把"读完"和"读懂"区分开——这是提升学习效果性价比最高的单一改动。
- ResourceCard 可以用来做"延伸阅读"卡片，把每篇末尾的纯文字链接列表升级成有视觉重量的卡片，帮助读者判断这条延伸内容值不值得点进去。

### 优先级2：进度可视化，制造"正在推进"的感知

- KnowledgeMap 目前只做"我在哪个模块"的定位，建议加入已读/未读的染色状态（哪怕只是基于 localStorage 的前端状态，不需要账号系统），让读者打开任意一篇文章都能看到整体完成度，这对长知识体系的完读率影响很大。
- 每个模块 index 页的"本模块文章"表格，可以加一列"已读"勾选状态，呼应 KnowledgeMap 的染色。

### 优先级3：专注模式再进一步

- 现有的"专注阅读"只是隐藏侧边栏，建议加一个可选的"渐进式阅读"——长文章（你的平均篇幅普遍在9000-23000字）默认按小节折叠，读完当前小节再展开下一节，避免读者一打开页面就被超长滚动条吓退，这对篇幅普遍偏长的 05-tools-frameworks 模块尤其有用。
- 代码块和 mermaid 图建议支持"点击放大"，现在 viewport 里长代码块和复杂架构图容易挤压阅读区域。

### 优先级4：视觉细节

- 当前主题色 `--vp-c-brand-1: #43546a` 偏冷灰蓝，专业感足够，但作为"知识地图/进度"这类需要视觉焦点的元素，建议单独定义一个强调色（比如做已读状态染色、Quiz正确反馈），不要复用品牌色，否则视觉层级会混在一起。
- 深色模式下 `--vp-c-brand-soft: rgba(196, 205, 215, 0.14)` 透明度较低，长文本阅读场景建议做一次深色模式下的对比度实测，尤其是代码块和普通正文的区分度。

---

## 六、执行路线图

| 阶段 | 时间 | 核心目标 |
|---|---|---|
| 阶段一 | 第1-2周 | Quiz/ResourceCard 接入现有文章；处理游离目录；建立 ROADMAP.md |
| 阶段二 | 第3-6周 | 补齐 03-memory、04-multi-agent 缺口（各补2-3篇） |
| 阶段三 | 第7-12周 | 新增 07-ontology、08-self-evolving-skills 模块骨架（各先出2篇核心文章） |
| 阶段四 | 第13-18周 | 新增 09-data-governance；06-eval-evolution 扩容 |
| 阶段五 | 持续 | 时效性复核机制常态化运行，每季度抽查一次"最后核实日期"超过6个月的文章 |

---

*本文档基于对仓库源码（`docs/` 目录、`.vitepress/theme/components/`、`prd.md`、`techd.md`）的实际读取生成，非基于首页文案的推测性建议。*
